<!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1 align="center">register page</h1>
    <form method="POST" action="/registerUser" align="center">
      <table class="" cellspacing="1" cellpadding="10" align="center">
        <thead>
          <tr>
            <th>Name</th>
            <th>
              <input type="text" name="name" />
            </th>
          </tr>
          <tr>
            <th>UserEmail</th>
            <th>
              <input type="text" name="email" />
            </th>
          </tr>
          <tr>
            <th>Password</th>
            <th>
              <input type="text" name="password" />
            </th>
          </tr>
        </thead>
      </table>
      <input type="submit" class="btn btn-secondary" value="register" />
      <p class="mt-3">Already have an account ?</p>
      <a href="/" class="btn btn-info">Login</a>
    </form>
  </body>
</html> -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register Page</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background: linear-gradient(
          to right,
          #f5f7fa,
          #c3cfe2
        ); /* Light pastel blue/gray gradient */
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .register-card {
        background-color: white;
        padding: 40px;
        border-radius: 20px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 450px;
      }

      .register-card h3 {
        margin-bottom: 30px;
        font-weight: bold;
        text-align: center;
        color: #333;
      }

      .form-control {
        border-radius: 15px;
        border: 1px solid #c3cfe2; /* Soft border color */
      }

      .btn-primary {
        background-color: #6a11cb; /* Soft purple similar to the login page */
        border: none;
        border-radius: 20px;
        padding: 12px;
        font-weight: bold;
        letter-spacing: 1px;
      }

      .btn-primary:hover {
        background-color: #2575fc; /* Soft blue for hover effect */
      }

      .text-muted {
        text-align: center;
        margin-top: 15px;
      }

      .form-label {
        color: #6a11cb; /* Using the soft purple from the login page */
        font-weight: bold;
      }

      .input-group-text {
        background-color: #6a11cb; /* Input group icon color */
        color: white;
        border: none;
      }

      .form-check-input:checked {
        background-color: #6a11cb;
        border-color: #6a11cb;
      }
    </style>
  </head>
  <body>
    <div class="register-card">
      <h3>Create an Account</h3>
      <form action="/registerUser" method="post">
        <div class="mb-3">
          <label for="name" class="form-label">Full Name</label>
          <input
            type="text"
            class="form-control"
            id="name"
            placeholder="Enter your name"
            name="name"
          />
        </div>
        <div class="mb-3">
          <label for="email" class="form-label">Email address</label>
          <div class="input-group">
            <span class="input-group-text">@</span>
            <input
              type="email"
              class="form-control"
              name="email"
              id="email"
              placeholder="Enter your email"
            />
          </div>
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input
            type="password"
            class="form-control"
            id="password"
            name="password"
            placeholder="Enter your password"
          />
        </div>
        <div class="mb-3">
          <label for="confirm-password" class="form-label"
            >Confirm Password</label
          >
          <input
            type="password"
            class="form-control"
            id="confirm-password"
            placeholder="Confirm your password"
            name="cpassword"
          />
        </div>

        <button type="submit" class="btn btn-primary w-100">Register</button>
      </form>
      <p class="text-muted mt-3">
        Already have an account? <a href="/">Login here</a>
      </p>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
